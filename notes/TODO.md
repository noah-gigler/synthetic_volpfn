- further synthetic data sources (heston etc)
- focus on uncertainty metrics (crps loss only run?)
- correlated noise


stretch goals:

- parameter uncertainty instead of pointwise uncertainty ()


UPDATE NOTEBOOKS TO NEW GRID METHOD